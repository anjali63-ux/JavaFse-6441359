// WordDocument implements the Document interface
public class WordDocument implements Document { //Interface - class(uses implements keyword)

    // This method defines what happens when we open a Word document
    public void meth1() {
        System.out.println("WORD File is now open and ready to read.");
    }
}
